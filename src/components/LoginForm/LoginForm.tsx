import React from 'react';
import {useForm} from "react-hook-form";
import {joiResolver} from "@hookform/resolvers/joi";

const LoginForm = () => {
    // const {register, reset, handleSubmit, setValue, formState: {isValid, errors}} = useForm({mode: 'all', joiResolver()});
    return (
        <div>
            LoginForm
        </div>
    );
};

export {LoginForm};