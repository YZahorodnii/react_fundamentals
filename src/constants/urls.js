const baseURL = 'https://jsonplaceholder.typicode.com/'

const posts = '/posts';
const comments = '/comments';

const urls = {
    posts: {
        posts: posts,
    },
    comments: {
        comments: comments
    }
};

export {
    baseURL,
    urls
}