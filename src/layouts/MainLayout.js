import React from 'react';
import {Comments, Header, Users} from "../components";

const MainLayout = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default MainLayout;